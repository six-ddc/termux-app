<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
     https://material.io/develop/android/theming/dark
    -->

    <!-- TermuxApp Light DarkActionBar theme. -->
    <style name="Theme.TermuxApp.Light.DarkActionBar" parent="Theme.BaseActivity.Light.DarkActionBar"/>
    <!-- TermuxApp Light NoActionBar theme. -->
    <style name="Theme.TermuxApp.Light.NoActionBar" parent="Theme.BaseActivity.Light.NoActionBar"/>

    <!-- TermuxApp DayNight DarkActionBar theme. -->
    <style name="Theme.TermuxApp.DayNight.DarkActionBar" parent="Theme.BaseActivity.DayNight.DarkActionBar"/>
    <!-- TermuxApp DayNight NoActionBar theme. -->
    <style name="Theme.TermuxApp.DayNight.NoActionBar" parent="Theme.BaseActivity.DayNight.NoActionBar"/>


    <!-- TermuxActivity DayNight NoActionBar theme. -->
    <style name="Theme.TermuxActivity.DayNight.NoActionBar" parent="Theme.TermuxApp.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>

        <item name="android:windowBackground">@color/black</item>

        <!-- Avoid action mode toolbar pushing down terminal content when
             selecting text on pre-6.0 (non-floating toolbar). -->
        <item name="android:windowActionModeOverlay">true</item>

        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:navigationBarColor">@color/termuxplus_surface</item>

        <!-- https://developer.android.com/training/tv/start/start.html#transition-color -->
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>

        <!-- Extra keys colors. -->
        <item name="extraKeysButtonTextColor">@color/white</item>
        <item name="extraKeysButtonActiveTextColor">@color/red_400</item>
        <item name="extraKeysButtonBackgroundColor">@color/black</item>
        <item name="extraKeysButtonActiveBackgroundColor">@color/grey_500</item>
    </style>

    <!-- Applied at runtime (before super.onCreate) when the terminal-transparency
         property is enabled: a translucent window that lets the system wallpaper
         show behind the translucent terminal surface. Inherits everything else
         from the base theme so day/night colours are unchanged. -->
    <style name="Theme.TermuxActivity.DayNight.NoActionBar.Transparent" parent="Theme.TermuxActivity.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- ============================================================= -->
    <!-- Termius-style modern bottom sheet (Material scaffold, custom skin) -->
    <!-- Used by TermuxPlusActionSheet and TermuxPlusTabOverview.       -->
    <!-- ============================================================= -->
    <style name="ShapeAppearance.TermuxPlus.Sheet" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">@dimen/termuxplus_sheet_radius</item>
        <item name="cornerSizeTopRight">@dimen/termuxplus_sheet_radius</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>

    <style name="Widget.TermuxPlus.BottomSheet" parent="Widget.Material3.BottomSheet.Modal">
        <item name="backgroundTint">@color/termuxplus_sheet_bg</item>
        <item name="shapeAppearance">@style/ShapeAppearance.TermuxPlus.Sheet</item>
        <item name="android:elevation">16dp</item>
    </style>

    <style name="Theme.TermuxPlus.BottomSheet" parent="Theme.Material3.DayNight.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/Widget.TermuxPlus.BottomSheet</item>
        <item name="colorPrimary">@color/termuxplus_accent</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <item name="colorSurface">@color/termuxplus_sheet_bg</item>
        <item name="colorOnSurface">@color/termuxplus_text_primary</item>
        <item name="android:backgroundDimAmount">0.55</item>
        <item name="android:navigationBarColor">@color/termuxplus_sheet_bg</item>
        <item name="enableEdgeToEdge">false</item>
    </style>

    <style name="Theme.TermuxPlus.SheetActivity" parent="Theme.TermuxActivity.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:navigationBarColor">@color/termuxplus_sheet_bg</item>
    </style>

</resources>
