package com.termux.autotermux.events.model

enum class EventType(val defaultEnabled: Boolean = true) {
    APP_FOREGROUND,
    APP_BACKGROUND,
    FOREGROUND_APP_CHANGED,
    ACTIVITY_CHANGED,
    WINDOW_STATE_CHANGED,
    WINDOW_CONTENT_CHANGED(defaultEnabled = false),
    VIEW_SCROLLED(defaultEnabled = false),
    ACCESSIBILITY_SERVICE_CONNECTED,
    ACCESSIBILITY_SERVICE_INTERRUPTED,
    ACCESSIBILITY_SERVICE_DISCONNECTED,
    CONFIGURATION_CHANGED,
    SCREEN_BOUNDS_CHANGED,
    LOCAL_SERVER_STARTED,
    LOCAL_SERVER_STOPPED,
    WEBSOCKET_CLIENT_CONNECTED,
    WEBSOCKET_CLIENT_DISCONNECTED,
    NOTIFICATION_POSTED,
    NOTIFICATION_REMOVED,
    NOTIFICATION_ACTION,
    NOTIFICATION_REPLY,
    BATTERY_LOW,
    BATTERY_OKAY,
    BATTERY_LEVEL_CHANGED,
    POWER_CONNECTED,
    POWER_DISCONNECTED,
    USER_PRESENT,
    NETWORK_CONNECTED,
    NETWORK_DISCONNECTED,
    NETWORK_TYPE_CHANGED,
    SMS_RECEIVED,
    PING,
    PONG,
    UNKNOWN
}
